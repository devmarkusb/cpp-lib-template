include_guard(GLOBAL)

# Add external config-package dependencies used by this project build here.
# Use find_package() in this file. If a dependency is exposed through this
# library's PUBLIC or INTERFACE link interface, mirror it in
# mb.cpp-lib-template-config.cmake.in with find_dependency().
#
# Example:
#
# if(NOT TARGET vendor::library)
#     find_package(vendor.library CONFIG REQUIRED)
# endif()
